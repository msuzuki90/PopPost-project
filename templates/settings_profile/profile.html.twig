{% extends 'base.html.twig' %}

{% block title %}Create Your Profile!{% endblock %}

{% block body %}
    {% include "nav.html.twig" %}
    {# {% block bread %} #}
        {# <div class="mx-10">
            → Create Your Profile!
        </div> #}
    {# {% endblock %} #}

    {{ include('settings_profile/_settings_profile_tabs.html.twig')}}
    {{ form_start(form) }}
        {{ include('settings_profile/_profile_form.html.twig') }}

        <div class="max-w-6xl mx-auto mt-10">
            <button type="submit" class="block w-full shadow-sm border-transparent bg-indigo-600 hover:bg-indigo-700 dark:bg-indigo-500 dark:hover:bg-indigo-400 text-white rounded-md border p-2 mt-4 mb-2">Create</button>
        </div>
    {{ form_end(form) }}
{% endblock %}

